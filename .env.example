# OpenAI-Zugang – diese Datei als ".env" kopieren und ausfüllen.
# WICHTIG: .env wird NICHT committet (siehe .gitignore) und liegt oberhalb
# des Web-Roots (public/), ist also nie per URL erreichbar.

OPENAI_API_KEY=sk-...
OPENAI_VECTOR_STORE_ID=vs_...
OPENAI_MODEL=gpt-4o
